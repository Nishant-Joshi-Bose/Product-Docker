#!/bin/bash
#
# An arbitrary collection of system logs to be
# delivered via the /logread.txt endpoint of the PtsServer.
#

if (( $# > 0 )); then
    echo >&2 "$0: unexpected argument '$1'"
    exit 1
fi

logread
printf "\n\f\n> dmesg\n"
dmesg

if [ -e /opt/Bose/etc/Shepherd-GVA.xml ]; then
    printf "\n\f\n> libassistant logs\n"
    cat /tmp/libassistant.log
fi

printf "\n\f\n"
pts-info
